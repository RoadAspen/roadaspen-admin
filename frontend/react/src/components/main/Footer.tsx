import React, { ReactElement } from 'react';
import * as mainStyle from '@/assets/css/main/main.scss';
function Footer(): ReactElement {
    return (
        <footer className={mainStyle.footerBox}>
            <p>黑恶嘿诶额呵呵按吉拉时间段拉手孔建档立卡</p>
        </footer>
    );
}

export default Footer;
